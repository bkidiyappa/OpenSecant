/**
 * Maps natural-language steps to Playwright code candidates.
 */
const {
  tryActionLibrary,
  getActionType,
} = require('../locator/locatorResolver');

module.exports = {
  tryActionLibrary,
  getActionType,
};
